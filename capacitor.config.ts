import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'isj.ing4.isi',
  appName: 'devmobileproject',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
